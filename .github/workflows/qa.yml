name: qa

on:
  workflow_dispatch:
  workflow_run:
    workflows: [build]
    branches: [feature/**]
    types:
      - completed

jobs:
  qa:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: build docker image, tag, build and push release image
        shell: bash
        run: cat "README.md";sleep 15
